from django.shortcuts import render
from .models import directores, peliculas

# no implemente eso por falta de codigo HTML
#esto crea una pagina html contador

#def index(request):
 #   num_directores = directores.objects.all().count()
  #  num_peliculas = peliculas.objects.all().count()

   # return render(
    #    request,
     #   'index.html',
      #  context={
       #     'num_directores' : num_directores,
        #    'num_peliculas' : num_peliculas
        #}
    #)
